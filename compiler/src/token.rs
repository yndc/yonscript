use crate::collections;

#[derive(Debug)]
pub enum Token {
    Identifier(String),
    Colon,
    Semicolon,
    Plus,
    Minus,
    Asterisk,
    ForwardSlash,
    If,
    Continue,
    For,
    Loop,
    While,
    Else,
    Return,
    Break,
    Match,
    True,
    False,
    Const,
    Function,
    Component,
    System,
    Reads,
    Mutates,
    Without,
    Filter,
}
